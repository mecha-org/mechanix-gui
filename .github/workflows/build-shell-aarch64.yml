name: Shell - Build aarch64
on:
  push:
    branches:
      - main
      - dev
      - dev-arm64-build

env:
  CARGO_INCREMENTAL: 0
  CARGO_NET_RETRY: 10
  RUST_BACKTRACE: short
  RUSTUP_MAX_RETRIES: 10
  MACOSX_DEPLOYMENT_TARGET: 10.7
  CARGO_NET_GIT_FETCH_WITH_CLI: true

jobs:
  build:
    name: Build
    strategy:
      fail-fast: true
      matrix:
        include:
          - name: mechanix-status-bar-aarch64-unknown-linux-gnu.tar.gz
            package: mechanix-status-bar
            path: shell

          - name: mechanix-greeter-aarch64-unknown-linux-gnu.tar.gz
            package: mechanix-greeter
            path: shell

          - name: mechanix-settings-panel-aarch64-unknown-linux-gnu.tar.gz
            package: mechanix-settings-panel
            path: shell

          - name: mechanix-lock-screen-aarch64-unknown-linux-gnu.tar.gz
            package: mechanix-lock-screen
            path: shell

          - name: mechanix-home-screen-aarch64-unknown-linux-gnu.tar.gz
            package: mechanix-home-screen
            path: shell

    runs-on: ubicloud-standard-2-arm
    continue-on-error: false
    env:
      RUSTFLAGS: ${{ matrix.rustflags || '' }}
    steps:
      - name: Setup | Checkout
        uses: actions/checkout@v4

      - name: Install Rust 1.75.0
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
          rustup default 1.75.0

      - name: Install cargo-deb
        run: cargo install cargo-deb

      - name: Update apt
        run: sudo apt update

      - name: Install dependencies
        run: |
          sudo apt install \
          libwayland-dev \
          libxkbcommon-dev \
          libdbus-1-dev

      - name: Set up Git SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY_GIT }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan github.com >> ~/.ssh/known_hosts

      - name: Build [${{ matrix.package }}]
        run: |
          cd ${{ matrix.path }}
          cargo --config net.git-fetch-with-cli=true fetch
          cargo build --release --package ${{ matrix.package }}
          ls -la

      - name: Package deb [${{ matrix.package }}]
        run: |
          cd ${{ matrix.path }}
          cargo deb --no-build --package ${{ matrix.package }}
          ls -la

      - name: Prepare artifacts [${{ matrix.package }}]
        run: |
          cd shell/target/release/
          tar czvf ../../../${{matrix.name}} ${{ matrix.package }}
          cd -
          cd shell/target/debian/
          mv *.deb ../../../
          cd -

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: ./mechanix*
